<form method="post" action="{{ path('functions_delete', {'id': function.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ function.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
