{% extends 'admin.html.twig' %}

{% block title %}Regoupement de Partenaires{% endblock %}

{% block body %}
    <h1>Regoupement de Partenaires</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ grouping_partner.id }}</td>
            </tr> #}
            <tr>
                <th>Regoupement de Partenaires sous ...</th>
                <td>{{ grouping_partner.titleGroupingPartner }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-secondary" href="{{ path('grouping_partners_index') }}">Retour</a>

    <a class="btn btn-warning" href="{{ path('grouping_partners_edit', {'id': grouping_partner.id}) }}">Modifier</a>

    {{ include('grouping_partners/_delete_form.html.twig') }}

{% endblock %}
