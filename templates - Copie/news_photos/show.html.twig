{% extends 'base.html.twig' %}

{% block title %}NewsPhotos{% endblock %}

{% block body %}
    <h1>NewsPhotos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ news_photo.id }}</td>
            </tr>
            <tr>
                <th>PhotoNewPhoto</th>
                <td>{{ news_photo.photoNewPhoto }}</td>
            </tr>
            <tr>
                <th>AltNewsPhoto</th>
                <td>{{ news_photo.altNewsPhoto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('news_photos_index') }}">back to list</a>

    <a href="{{ path('news_photos_edit', {'id': news_photo.id}) }}">edit</a>

    {{ include('news_photos/_delete_form.html.twig') }}
{% endblock %}
