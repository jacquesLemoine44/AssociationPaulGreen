
{# {% if app.user.roles[0]=="ROLE_ADMIN" %}
    {% extends 'admin.html.twig' %}
 {% else %}    
    {% extends 'base.html.twig' %}
{% endif %}  #}

{% extends (app.user.roles[0]=="ROLE_ADMIN") ? 'admin.html.twig' : 'base.html.twig' %}


{% block title %}Modification des Adhérents{% endblock %}

{% block body %}
    <h1>Modification des Adhérents</h1>

    {{ include('users/_form.html.twig', {'button_label': 'Valider'}) }}

    <a class="btn btn-secondary" href="{{ path('users_index') }}">Retour</a>

    {{ include('users/_delete_form.html.twig') }}

{% endblock %}
