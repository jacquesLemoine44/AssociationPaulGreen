{% extends 'admin.html.twig' %}

{% block title %}Affectations{% endblock %}

{% block body %}
    <h1>Affectations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Affectation</th>
                <td>{{ affectation.nameaffectation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ affectation.descriptionaffectation }}</td>
            </tr>
        </tbody>
    </table>

    <hr class="mb-3">
    <div class="d-flex flex-row my-2">
        
        <a class="btn btn-grad btn-secondary2 me-2" href="{{ path('app_affectations_index') }}">Retour</a>
        <a class="btn btn-grad btn-warning2 me-2" href="{{ path('app_affectations_edit', {'id': affectation.id}) }}">Modifier</a>
        {{ include('affectations/_delete_form.html.twig') }}

{% endblock %}
