{% extends 'admin.html.twig' %}

{% block title %}Modifier un message de conatct{% endblock %}

{% block body %}
    <h1>Modifier le message de contact</h1>

    {{ include('contacts/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-grad btn-secondary2" href="{{ path('contacts_index') }}">Retour</a>

    {{ include('contacts/_delete_form.html.twig') }}
{% endblock %}
