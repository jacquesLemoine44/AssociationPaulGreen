<form method="post" action="{{ path('app_doc_actions_assos_delete', {'id': doc_actions_asso.id}) }}" 
    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doc_actions_asso.id) }}">
    <button class="btn btn-grad btn-danger2 me-2">Supprimer</button>
</form>
