{% extends 'admin.html.twig' %}

{% block title %}Modifier une Sortie Universitaire{% endblock %}

{% block body %}
    <h1>Modifier une Sortie Universitaire</h1>

    {{ include('fieldtrips/_form.html.twig', {'button_label': 'Valider'}) }}

    <a class="btn btn-grad btn-secondary2 me-2"href="{{ path('app_fieldtrips_index') }}">Retour</a>

    {{ include('fieldtrips/_delete_form.html.twig') }}

{% endblock %}
