<form method="post" action="{{ path('functions_delete', {'id': function.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ function.id) }}">
        <button class="btn btn-grad btn-danger2 me-2">Supprimer</button>

    </div>
    <hr class="mb-3">

</form>
