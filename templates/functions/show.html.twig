{% extends 'admin.html.twig' %}

{% block title %}Fonctions{% endblock %}

{% block body %}
    <h1>Fonctions</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ function.id }}</td>
            </tr> #}
            <tr>
                <th>Fonction</th>
                <td>{{ function.mission }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ function.descriptionFunction }}</td>
            </tr>
        </tbody>
    </table>

    <hr class="mb-3">

    <div class="d-flex flex-row my-2">
    
        <a class="btn btn-secondary me-1" href="{{ path('functions_index') }}">Retour</a>
        <a class="btn btn-warning me-1" href="{{ path('functions_edit', {'id': function.id}) }}">Modifier</a>
        {{ include('functions/_delete_form.html.twig') }}

{% endblock %}
