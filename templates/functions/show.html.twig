{% extends 'admin.html.twig' %}

{% block title %}Functions{% endblock %}

{% block body %}
    <h1>Functions</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ function.id }}</td>
            </tr> #}
            <tr>
                <th>Fonction</th>
                <td>{{ function.mission }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ function.descriptionFunction }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('functions_index') }}">Retour</a>

    <a class="btn btn-warning" href="{{ path('functions_edit', {'id': function.id}) }}">Modifier</a>

    {{ include('functions/_delete_form.html.twig') }}


{% endblock %}
