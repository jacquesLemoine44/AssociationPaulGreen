{% extends 'admin.html.twig' %}

{% block title %}Modifier un lien vers un site{% endblock %}

{% block body %}
    <h1>Modifier un lien vers un site</h1>

    {{ include('link_actions_assos/_form.html.twig', {'button_label': 'Valider'}) }}

    {% if app.request.attributes.get('_route') == 'mod_link_actions_assos' %}
        <form>
            <input class="btn btn-secondary me-2" type="button" value="Retour" onclick="history.back()">
        </form>
    {% else %}
        <a class="btn btn-grad btn-secondary2 me-2" href="{{ path('app_link_actions_assos_index') }}">Retour</a>
    {% endif %}     

    {{ include('link_actions_assos/_delete_form.html.twig') }}

{% endblock %}
