<form method="post" action="{{ path('news_photos_delete', {'id': news_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_photo.id) }}">
    <button class="btn btn-grad btn-danger2 me-2">Supprimer</button>
</form>
