{% extends 'admin.html.twig' %}

{% block title %}Photos des News{% endblock %}

{% block body %}
    <h1>Photos des News</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Photo</th>
                <td>{{ news_photo.photoNewPhoto }}</td>
            </tr>
            <tr>
                <th>Texte Alternatif</th>
                <td>{{ news_photo.altNewsPhoto }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-grad btn-secondary2 me-2" href="{{ path('news_photos_index') }}">Retour</a>

    <a class="btn btn-grad btn-warning2 me-2" href="{{ path('news_photos_edit', {'id': news_photo.id}) }}">Modifier</a>

    {{ include('news_photos/_delete_form.html.twig') }}
    
{% endblock %}
