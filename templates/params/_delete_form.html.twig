<form method="post" action="{{ path('params_delete', {'id': param.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ param.id) }}">
    <button class="btn btn-grad btn-danger2 me-2">Supprimer</button>
</form>
