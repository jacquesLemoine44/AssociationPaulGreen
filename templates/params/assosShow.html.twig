{% extends 'admin.html.twig' %}

{% block title %}Paramètres Principaux{% endblock %}

{% block body %}

    <h1 class="text-center bg-logoGreen1">Présentation de l'association</h1>

    <div>
        <a class="btn btn-grad btn-warning2" 
            href="{{ path('params_assosedit', {'id': param.id}) }}">Modifier</a>
        <hr class="mb-3">
    </div>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Présentation de l'association</th>
                <td>{{ param.assosparam | raw }}</td>
            </tr>
 
        </tbody>
    </table>

{% endblock %}
