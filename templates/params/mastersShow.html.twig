{% extends 'admin.html.twig' %}

{% block title %}Paramètres Principaux{% endblock %}

{% block body %}

    <h1>Présentation du Master</h1>

    <div>
        <a class="btn btn-grad btn-warning2" 
            href="{{ path('params_mastersedit', {'id': param.id}) }}">Modifier</a>
        <hr class="mb-3">
    </div>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Présentation du Master :</th>
                <td>{{ param.masterparam | raw }}</td>
            </tr>
 
        </tbody>
    </table>

{% endblock %}
