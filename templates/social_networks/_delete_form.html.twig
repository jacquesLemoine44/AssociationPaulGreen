<form method="post" action="{{ path('social_networks_delete', {'id': social_network.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social_network.id) }}">
        <button class="btn btn-danger me-2">Supprimer</button>
        
    </div>
    <hr class="mb-3">

</form>
