{{ form_start(form) }}

    {# {{ form_widget(form) }} #}
    {{ form_row(form.socialNetwork) }}

    {# {{ form_row(form.logoSocialnetwork) }} #}

    <div class="mb-3">
        {{ form_label(form.codeSocialnetwork) }}
        {% if app.request.attributes.get('_route') == 'social_networks_edit' %}
            {% if (social_network.codeSocialnetwork) %}
                {% set xlogo = social_network.codeSocialnetwork %}
                <i class="{{ xlogo }}"></i>
            {% endif %} 
        {% endif %} 
        {{ form_widget(form.codeSocialnetwork) }}
    </div>

    <div class="mb-3">
        {{ form_label(form.linkSocialnetwork) }}
        {% if ( social_network.linkSocialnetwork ) %} 
            <a href="{{ social_network.linkSocialnetwork }}"><i class="fas fa-link"></i></a>                       
        {% else %}
            <i class="fas fa-unlink"></i>
        {% endif %} 
        {{ form_widget(form.linkSocialnetwork) }}
    </div>


	{# {% if app.request.attributes.get('_route') == 'social_networks_edit' %}
            <div>
                {% if (social_network.logoSocialnetwork) %}
                    {% set xlogo = social_network.logoSocialnetwork %}
                    <p>logo :  {{ xlogo | raw }}</p>
                {% endif %}                

                {% if (social_network.codeSocialnetwork) %}
                    {% set xlogo = social_network.codeSocialnetwork %}
                    <p>Code logo :  <i class="{{ xlogo }}"></i> </p>
                {% endif %}    
            </div>
    {% endif %} #}

    <hr class="mb-3">
    <div class="d-flex flex-row my-2">

        <button class="btn btn-success me-2">{{ button_label|default('Sauver') }}</button>

{{ form_end(form) }}
