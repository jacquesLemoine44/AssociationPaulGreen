<form method="post" action="{{ path('themes_delete', {'id': theme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
        <button class="btn btn-grad btn-danger2 me-2">Supprimer</button>
        
    </div>
    <hr class="mb-3">
</form>
