
{# {% if app.user.roles[0]=="ROLE_ADMIN" %}
    {% extends 'admin.html.twig' %}
 {% else %}    
    {% extends 'base.html.twig' %}
{% endif %}  #}

{% extends (app.user.roles[0]=="ROLE_ADMIN") ? 'admin.html.twig' : 'base.html.twig' %}


{% block title %}
    Modification des Adhérents
{% endblock %}

{% block body %}
    <div>
        <h1 class="text-center text-succes2">Modification des Adhérents</h1>
    </div>

        {{ include('users/_form.html.twig', {'button_label': 'Valider'}) }}
        {# <div class="d-flex flex-column justify-content-start"> #}
            <a class="btn btn-secondary me-2" href="{{ path('users_index') }}">Retour</a>
        {# </div> #}
        {{ include('users/_delete_form.html.twig') }}

{% endblock %}
